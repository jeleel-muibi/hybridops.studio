# configure_bgp (network role)

Skeleton role to render and push basic BGP config. Ships a Cisco IOS example; extend with NX‑OS, EOS, Junos tasks/templates as needed.

Variables: `device_os`, `asn`, `neighbors`, `vrf`.
