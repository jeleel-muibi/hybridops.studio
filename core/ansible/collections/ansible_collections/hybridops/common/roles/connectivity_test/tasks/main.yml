---
# SPDX-License-Identifier: MIT-0
# Maintainer: HybridOps.Studio
# Created: 2025-09-09 21:39:57 UTC
# Updated: 2025-09-10 — Initial orchestration of connectivity result compilation
#
# Purpose: Orchestrate: per-host tests → per-host compile → central save (once).

- name: Execute connectivity tests (per host)
  include_tasks: run_tests.yml

- name: Compile test results (per host)
  include_tasks: compile_results.yml

- name: Save results centrally (JSON + JSONL) and publish run metadata
  include_tasks: save_results.yml
