ansible_user: root
ansible_password: "{{ vault_default_password }}"  # Use Ansible Vault for security
ansible_ssh_port: 22
ansible_become: false

# Add any NAS-specific variables below
nas_mount_point: /mnt/storage
nas_share_name: data
nas_protocol: nfs
