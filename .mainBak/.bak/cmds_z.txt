source .venv/bin/activate

ln -s /home/user/hybridops-studio/inventories/dev/* /home/user/hybridops-studio/inventories/staging/

./z_deployments/deploy.sh dev
ansible-playbook common/playbook/gen_inventory.yml

To create roles:
    cd roles
    for role in vlan ospf int_config bgp ntp vrrp hsrp; do
      ansible-galaxy init $role
    done


Make sure deploy.sh is executable:
 - chmod +x deploy.sh
 -  chmod +x ./deployments/deploy.sh

ansible_python_interpreter: "{{ ansible_playbook_python }}" : This avoids interpreter warnings and ensures local execution uses your virtualenv.
      mapping (it will point to the Python interpreter of your current virtual environment.)
