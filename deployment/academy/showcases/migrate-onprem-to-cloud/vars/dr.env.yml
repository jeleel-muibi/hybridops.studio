# Scenario overrides for DR demo (example placeholders)
dr_dns_zone: "corp.example.com"
dr_cutover_ttl: 30
dr_primary_region: "uk-south"
dr_secondary_region: "westeurope"
# add app-specific flags here if needed
